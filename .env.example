# Fitness App – Example Environment Variables
# Copy this file to ".env" and fill in the values as needed.

# --- OpenAI (optional for full agent runs) ---
# Your OpenAI API key. If not set, the script will init the DB and skip agent calls.
OPENAI_API_KEY=

# --- Database (Postgres only) ---
DATABASE_URL=postgresql://fitness_user:fitness_password@localhost:5432/fitness_app

# --- Docker Compose service variables ---
# Used by docker-compose.yml to configure containers
# Postgres container
POSTGRES_DB=fitness_app
POSTGRES_USER=fitness_user
POSTGRES_PASSWORD=fitness_password

# pgAdmin container
PGADMIN_DEFAULT_EMAIL=admin@fitness-app.com
PGADMIN_DEFAULT_PASSWORD=admin_password

# --- Demo Identity (for sample runs) ---
DEMO_USER_ID=user_123
DEMO_USER_NAME=Alex
DEMO_USER_TZ=UTC
DEMO_SESSION_ID=fitness_chat


